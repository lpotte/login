package com.example.loginwithmvvm.repository

import androidx.lifecycle.LiveData
import androidx.lifecycle.MutableLiveData
import com.example.loginwithmvvm.sharedPreference.Provider

class repositoryStatus {

    /*private val sharedPreferences = Provider
    var status = MutableLiveData<Boolean>()
    var stateLogged : Boolean = false

    fun signIn(){
        sharedPreferences.signIn()
        stateLogged = true
    }
    fun singout(){
        sharedPreferences.signOut()
        stateLogged = false
    }

    init {
        stateLogged = sharedPreferences.getStatus()!!
        status.value = stateLogged
    }

    fun getStatus() = status.value
*/
}